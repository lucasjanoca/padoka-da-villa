# 2026-08-20 17:34 — Retry seguro do Caixa / PDV

- Relidos `README.md`, `CHANGELOG.md` e `AUTH_STATUS.md` antes da alteração e revisado o estado mais recente do repositório.
- Confirmado que a execução anterior já havia preparado a migration `supabase/010_pdv_sale_idempotency.sql` e a auditoria SQL correspondente, mas o `pdv.html` publicado ainda usava `padoka_create_sale` sem `request_id`.
- Criado `assets/pdv-idempotency.js`, carregado somente na página interna do PDV por `assets/internal-nav.js`.
- A nova camada só assume a finalização quando detecta a coluna `request_id` em `padoka_sales`; antes da migration 010 existir, o fluxo atual permanece intacto para não quebrar o publicado.
- Quando a migration 010 estiver ativa, novas vendas usam `padoka_create_sale_once` com um UUID por tentativa.
- Em resposta ambígua/rede instável, itens, forma de pagamento e `request_id` ficam preservados em `sessionStorage`; a interface bloqueia alterações da venda e oferece **Tentar novamente** com exatamente a mesma operação.
- Ao recarregar a página com uma tentativa pendente, o carrinho é reconstruído a partir do snapshot preservado e continua usando o mesmo `request_id`, reduzindo o risco de dupla venda/dupla baixa de estoque.
- Erros determinísticos como estoque insuficiente, permissão, item inválido ou conflito de `request_id` liberam a tentativa para revisão em vez de manter um retry impossível.
- `tests/pdv-idempotency-audit.mjs` foi ampliado para auditar também o frontend: carregamento condicional, uso da RPC idempotente, persistência em `sessionStorage`, reutilização do mesmo `request_id`, ausência de escrita direta no estoque e ausência de fallback para venda local.
- `node --check` do novo `assets/pdv-idempotency.js` passou localmente.
- O commit final ainda não apresentou status/check remoto pelo conector do GitHub, portanto a CI remota não foi marcada como concluída sem evidência.
- O conector Supabase continua expondo somente **InfoTech.io** (`rgngqumqzylthdiazvfu`). Nenhuma query, migration, advisor ou alteração foi executada nele.
- O backend correto da PADOKA permanece **Sites De Clientes!** (`yncspxfsvlqdnodlsosb`); a migration 010 continua somente preparada até que esse projeto esteja disponível para aplicação e revisão.
