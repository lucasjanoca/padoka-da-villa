# 2026-08-20 20:26 — Estorno transacional do Caixa / PDV preparado

- Relidos `README.md`, `CHANGELOG.md`, `AUTH_STATUS.md` e o estado atual das migrations/PDV antes da alteração.
- Confirmado pelo conector Supabase que a única conexão disponível continua sendo **InfoTech.io** (`rgngqumqzylthdiazvfu`). Nenhuma query, migration, advisor ou alteração foi executada nele; o backend correto da PADOKA permanece **Sites De Clientes!** (`yncspxfsvlqdnodlsosb`).
- Criada `supabase/012_pdv_sale_void_transaction.sql`, dependente das migrations 003/004 e destinada exclusivamente ao backend correto da PADOKA.
- A migration adiciona `void_reason` a `padoka_sales` e a RPC `padoka_void_sale(uuid,text)`.
- O estorno fica restrito a `owner` e `manager`, exige motivo válido e bloqueia a venda com `FOR UPDATE` antes de qualquer devolução de estoque.
- Uma venda já `voided` retorna sem devolver estoque novamente, tornando retries do estorno idempotentes e evitando dupla reposição.
- As linhas de `padoka_inventory` são bloqueadas em ordem estável, a existência do estoque é conferida para todos os itens e a devolução acontece na mesma transação do estorno.
- Cada devolução cria um movimento positivo em `padoka_inventory_movements`, com referência à venda original e motivo auditável; a venda registra `voided_by`, `voided_at` e `void_reason`.
- `anon/public` não recebem execução da RPC e nenhum `UPDATE` direto em `padoka_sales` foi concedido ao frontend.
- Criado `tests/pdv-sale-void-audit.mjs` e adicionado ao workflow `.github/workflows/padoka-audit.yml` para proteger permissões, lock, idempotência, devolução de estoque, auditoria e ausência de trigger global em `auth.users`.
- A tentativa de executar o teste localmente foi bloqueada pelo DNS do ambiente ao resolver `raw.githubusercontent.com`; por isso a CI remota não é marcada como aprovada sem evidência.
- Nenhuma migration/RLS foi aplicada nesta execução; portanto não havia advisor de segurança da PADOKA a consultar nesta rodada.
